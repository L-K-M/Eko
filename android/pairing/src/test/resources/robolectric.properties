# Unit-test manifests carry no targetSdk, so the picker defaults to
# compileSdk (37) whose android-all jar requires Java 21 while CI runs
# JDK 17. SDK 35 is the newest sandbox that still runs on Java 17.
sdk=35
